<h1>id: <%= @word.id %> の単語編集ページ</h1>

<%= form_with(model: @word, local: true) do |f| %>
  <%= f.label :content, '単語' %>
  <%= f.text_field :content %>

  <%= f.submit '投稿' %>
<% end %>

<%= link_to '一覧に戻る', words_path %>
